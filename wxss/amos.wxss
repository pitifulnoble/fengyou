/**
 点击效果：向右下方小幅移动。
 */
.rect-hover {
    position: relative;
    top: 3rpx;
    left: 3rpx;
    box-shadow: 0px 0px 8px rgba(0, 0, 0, 1) inset;
}

/**
 点击效果：图标小幅缩小，图标较大时不建议使用(大图标使用rect-hover)。
 */
.small-hover {
    opacity: 0.9;
    transform: scale(0.95, 0.95);
}

/**
 点击效果：图标中幅缩小，使用较少。
 */
.medium-hover {
    opacity: 0.8;
    transform: scale(0.85, 0.85);
}
